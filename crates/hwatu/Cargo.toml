[package]
name = "hwatu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Visual verification browser for AI coding agents: real WebKit rendering, ~13ms window spawn, MCP server"
keywords = ["browser", "webkit", "agent", "mcp", "verification"]
categories = ["web-programming", "development-tools", "command-line-utilities"]
readme = "../../README.md"

[dependencies]
hwatu-ipc = { path = "../ipc", version = "0.7.1" }
serde_json = "1"
# `hwatu clone` crops engine screenshots for blank WebGL canvases.
png = "0.17"
